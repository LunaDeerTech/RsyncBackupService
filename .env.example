RBS_JWT_SECRET=change-me-to-a-random-string
RBS_DATA_DIR=./data
RBS_PORT=8080
RBS_WORKER_POOL_SIZE=3
RBS_LOG_LEVEL=info
RBS_DEV_MODE=false
RBS_GOPROXY=https://proxy.golang.org,direct

# Optional SMTP settings for password delivery and notification email.
RBS_SMTP_HOST=
RBS_SMTP_PORT=
RBS_SMTP_USERNAME=
RBS_SMTP_PASSWORD=
RBS_SMTP_FROM=